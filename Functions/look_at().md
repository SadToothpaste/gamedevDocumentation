The look_at() function rotates your node in the direction of the vector that's within the parentheses'.

How to use look_at() in 2D:
```
look_at(Vector2(x, y))
```

How to use look_at() in 3D:
```
look_at(Vector3(x, y, z))
```